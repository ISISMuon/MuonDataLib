[build-system]
requires = ['cython', 'numpy', 'setuptools<=75.7.0', 'pyqt6==6.7.1']
build-backend = 'setuptools.build_meta'

[tool.setuptools]
package-dir = {"" = "src"}


[tool.setuptools.packages.find]
where = ["src"]

[project]
name = 'muondatalib'

version = "0.9.2b5"

requires-python = ">=3.8"
dependencies = ['numpy', 'h5py', 'plotly', 'cython', 'pyqt6==6.7.1', 'dash', 'dash_bootstrap_components']
authors = [{name='Anthony Lim', email='anthony.lim@stfc.ac.uk'}]
description = 'Reading and writing for ISIS muon experiments.'
readme = 'README.md'
license = {text = 'BSD'}

